import "package:flutter/material.dart";

class HeroImageScreenArgs {
  final String tag;
  final String filePath;
  final String imageURL;

  HeroImageScreenArgs(this.tag, this.filePath, this.imageURL);
}
